{
  "version": 2,
  "builds": [
    { "src": "backend/api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/api/$1" }
  ],
  "build": {
    "env": {
      "VERCEL_IGNORE_BUILD_CACHE": "1"
    }
  }
}
